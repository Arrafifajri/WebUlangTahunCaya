{
  "name": "web-ultah-caya",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev . --d1 SETTINGS_DB --kv SETTINGS_KV",
    "check": "node --check script.js && node --check dashboard.js",
    "deploy": "wrangler pages deploy . --project-name=cayawulandari"
  }
}
